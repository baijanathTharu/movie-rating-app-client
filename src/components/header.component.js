import { Link } from 'react-router-dom';

const navLinks = [
  {
    path: '/about',
    name: 'About',
  },
  {
    path: '/services',
    name: 'Services',
  },
  {
    path: '/contact',
    name: 'Contact',
  },
];

export const Header = () => {
  const navList = navLinks.map(({ path, name }, idx) => {
    return (
      
    );
  });

  return (
  
  );
};
